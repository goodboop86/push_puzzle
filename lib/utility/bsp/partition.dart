// 再帰構造を持つツリークラス
class Partition {
  // 分割された長方形
  late List<List<int>> rect;
  late Partition children;
  late int depth;

  Partition({required this.rect, required this.children, required this.depth}) {
    // TODO: implement Partition
    throw UnimplementedError();
  }
}